{
  "name": "@drorgl/aho-corasick2",
  "version": "1.0.2",
  "description": "Aho–Corasick string matching algorithm",
  "keywords": [
    "Aho-Corasick automation",
    "ac",
    "ac automation",
    "aca",
    "add",
    "aho",
    "aho-corasick",
    "algorithm",
    "array",
    "automata",
    "corasick",
    "data",
    "javascript",
    "match",
    "multiple",
    "pattern",
    "remove",
    "stream",
    "strings",
    "structure",
    "tree",
    "tree-structure",
    "trie",
    "trie-structure",
    "words"
  ],
  "homepage": "https://github.com/drorgl/aho-corasick#readme",
  "bugs": {
    "url": "https://github.com/drorgl/aho-corasick/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drorgl/aho-corasick.git"
  },
  "license": "ISC",
  "author": "Dejian Xu <xudejian2008@gmail.com> (https://github.com/xudejian/aho-corasick)",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "mocha -r ts-node/register tests/**/*.spec.{ts,tsx}\"",
    "performance-test": "node --prof node_modules/mocha/bin/_mocha -r ts-node/register tests/**/*.spec.ts",
    "performance-process": "node --prof-process isolate...",
    "test-one": "mocha -r ts-node/register",
    "lint": "tslint --project .",
    "dryrun": "tsc -noEmit",
    "build": "tsc",
    "build:clean": "rimraf ./dist",
    "precommit": "npm run lint && npm run dryrun && npm run test",
    "test:ci": "mocha --reporter mocha-junit-reporter -r ts-node/register tests/**/*.spec.ts",
    "doc:clean": "rimraf ./docs/html",
    "doc": "npm run doc:clean && typedoc --tsconfig tsconfig.json  --plugin none",
    "doc:markdown": "npm run doc:clean && typedoc --tsconfig tsconfig.json --plugin typedoc-plugin-markdown --theme markdown && concat-md --toc --decrease-title-levels --dir-name-as-title ./docs/html > ./docs/html/single-page.md",
    "publish-now": "npm publish --access public",
    "coverage": "rimraf ./coverage && nyc mocha -r ts-node/register -r source-map-support/register --ui bdd tests/**/*.spec.ts"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    "concat-md": "^0.3.5",
    "@types/chai": "^4.2.11",
    "@types/mocha": "^7.0.2",
    "@types/node": "^14.0.14",
    "chai": "^4.2.0",
    "mocha": "^8.0.1",
    "mocha-junit-reporter": "^2.0.0",
    "nyc": "^15.1.0",
    "rimraf": "^3.0.2",
    "ts-node": "^8.10.2",
    "tslint": "^6.1.2",
    "typedoc": "^0.17.7",
    "typedoc-plugin-markdown": "^2.3.1",
    "typescript": "^3.9.5"
  }
}
